# ==============================================================================
# OdysseyWM Input Configuration
# Navigate Beyond
# ==============================================================================

# Touchpad Settings
input type:touchpad {
    tap enabled
    natural_scroll enabled
    middle_emulation enabled
    dwt enabled
}

# Keyboard Settings
input type:keyboard {
    xkb_layout us
    repeat_delay 300
    repeat_rate 50
}
